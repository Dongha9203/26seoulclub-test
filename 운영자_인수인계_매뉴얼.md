# 동아리ON 챗봇 — 운영자 운영 셋업 매뉴얼

> 이 문서는 챗봇/관리자 대시보드 "사용법"이 아니라, 운영자가 시스템을 직접
> 초기 설정하고 유지보수할 수 있도록 정리한 가이드입니다. (대시보드 사용법은
> 별도 사용자 매뉴얼을 참고하세요.)

## 0. 현재 운영 환경 한눈에 보기

| 항목 | 현재 상태 | 처리 사항 |
|---|---|---|
| Supabase (DB) | 운영자 계정 소유 | 그대로 사용 — 추가 작업 없음 |
| Anthropic API 키 | 운영자 계정 소유 | 그대로 사용 |
| Voyage AI API 키 | 운영자 계정 소유 | 그대로 사용 |
| Notion 통합(integration) | 운영자 워크스페이스에 생성됨 | 그대로 사용 — 단, **새 페이지를 만들 때마다 공유 필요** (4-a 참고) |
| Voyage AI 결제수단 | **미등록** | 등록 필요 (4-f 참고) — 안 하면 노션 동기화 시 임베딩이 조용히 실패할 수 있음 |
| GitHub 저장소 | 최신 코드는 `seoulclub-on/26seoulclub`에 있음 | 저장소에 최신 코드가 있는지 확인 |
| Vercel 배포 | 아직 미배포 | 운영자 Vercel 계정에 새 프로젝트로 처음부터 연결 |

핵심: DB/API 키/Notion은 이미 운영자 소유라 옮길 게 없습니다. **실제로 옮겨야 하는 건
"코드"와 "그 코드를 띄우는 Vercel 프로젝트" 두 가지뿐**입니다.

---

## 1. 초기 설정 전 확인해야 할 것

1. **코드**: GitHub 저장소 `seoulclub-on/26seoulclub`의 기본 브랜치에 최신 코드가
   있는지 확인합니다. (이 저장소는 운영자 소유입니다.)

2. **Vercel에 입력할 환경변수 6종**: 아래 값을 안전하게 보관하고, Vercel 배포 시
   직접 입력합니다. 카카오톡·이메일 등에 평문으로 저장하지 마세요.
   - `NOTION_API_TOKEN`
   - `ANTHROPIC_API_KEY`
   - `VOYAGE_API_KEY`
   - `SUPABASE_DB_URL`
   - `JWT_SECRET_KEY` — **운영 배포용으로 새로 발급** (테스트용 키 재사용 금지).
     발급 명령: `python -c "import secrets; print(secrets.token_urlsafe(48))"`
   - `AIRTABLE_API_TOKEN` — 현재는 미사용. 향후 Airtable 직접 연동(B안) 적용 시 필요.
     (a-1 항목 참고)

3. **최초 관리자 계정**: 가입 화면이 따로 없으므로 터미널에서 아래 명령으로 직접 생성합니다.
   ```
   python create_admin_account.py <이메일> <비밀번호>
   ```

4. 이 문서.

---

## 2. Vercel에 배포하기 (최초 1회)

1. [vercel.com](https://vercel.com) 에 본인(운영자) 계정으로 로그인
2. **Add New...** → **Project**
3. GitHub 저장소 `seoulclub-on/26seoulclub` 선택해서 Import
   - 목록에 안 보이면: GitHub → **Settings** → **Applications** → **Vercel** → **Configure**에서
     Repository access에 `26seoulclub`이 추가되어 있는지 확인 (없으면 추가 후 Save)
4. Framework Preset: 자동 감지 안 되면 **Other** 선택
5. **Environment Variables**에 1번에서 확인한 환경변수 값을 모두 입력
   (Production / Preview / Development 전부 체크)
6. **Deploy** 클릭
7. 배포 완료 후 나온 주소(예: `https://26seoulclub.vercel.app`) 확인

---

## 3. 배포 후 1회만 하면 되는 작업

1. **챗봇 동작 확인**: `{배포주소}/widget.html` 접속해서 질문 1개 보내보기
2. **관리자 로그인 확인**: `{배포주소}/admin/login.html`에서 1-3번 계정으로 로그인
3. 둘 중 하나라도 안 되면 → Vercel 대시보드의 **Deployments → 실패 로그**를
   확인하고, 환경변수 5종이 모두 올바르게 입력됐는지 재검토하세요.
4. 로그인 성공하면 대시보드 내 **비밀번호 변경** 메뉴에서 전달받은 비밀번호를
   본인만 아는 비밀번호로 즉시 변경
5. **노션 임베드 주소 교체**: 기존에 노션 페이지에 넣어둔 위젯 임베드 블록의 URL을
   `{새 배포주소}/widget.html`로 바꿔주기 (배포 계정이 바뀌면 주소도 바뀝니다)
6. (선택) Vercel 프로젝트 설정에서 커스텀 도메인 연결 가능 — 원하면 진행

---

## 4. 운영 중 알고 있어야 할 것

### a) 노션에 새 정보를 추가할 때
- 챗봇은 **"메인페이지(2026 서울 동아리ON)"** 1개를 최상위로 수집하며, 그 안에 있는
  모든 하위 페이지(예: "통합시스템(필독)")도 자동으로 함께 수집됩니다.
  하위 페이지/토글/섹션을 추가하는 건 자동으로 챗봇에 반영됩니다(매일 새벽 자동
  갱신 또는 대시보드 "지금 갱신" 버튼).

> **참고 — config.json의 `integrated_system` 항목이 비어 있는 이유**
>
> "통합시스템(필독)" 노션 페이지는 메인페이지의 하위 페이지로 이미 포함되어 있습니다.
> 초기에는 이 페이지 URL을 `config.json`의 `integrated_system`에 별도로 등록했으나,
> 메인페이지 수집 시 한 번, `integrated_system` URL로 한 번, 총 2번 수집되어
> DB에 중복 저장되는 문제가 확인되었습니다.
> 이를 방지하기 위해 `integrated_system` 값을 `""`(빈 값)으로 변경하였으며,
> 이후에는 메인페이지 탐색 과정에서 1번만 수집됩니다.
> **이 빈 값을 다시 채우지 마세요.**
- 단, 그 페이지가 **다른 사람이 새로 만들었거나 워크스페이스 다른 곳에서 옮겨온
  페이지**라면, 그 페이지를 열어 우측 상단 **"..." → "연결(Connections)"**에
  챗봇 통합이 추가되어 있는지 꼭 확인하세요. 빠지면 챗봇이 그 내용을 영원히
  찾지 못합니다. (실제로 "강사비 지급 기준" 페이지가 이 문제였습니다.)
- 완전히 새로운 **최상위** 페이지(메인페이지/통합시스템과 같은 레벨)를 추가로
  연결하려면 `config.json`의 `notion_pages` 항목에 새 URL을 추가하고 Vercel에
  재배포하면 됩니다. (4-e 항목 참고)
- **Airtable과 노션 데이터베이스는 서로 다른 기능**이고, 챗봇이 읽는 범위도 다릅니다.
  - 노션 페이지에 **Airtable 표/폼을 임베드**해 넣은 경우 → 표 안 데이터는
    못 읽고, 그 자리에 있던 링크만 "관련 링크: https://..." 형태로 검색에 남습니다.
  - 노션 자체의 **데이터베이스(표 보기) 기능**으로 만든 자료(예: "자료실")는
    각 행(row)을 별도 페이지로 보고 그 안의 내용을 챗봇이 읽어 검색에
    활용합니다. 행 추가/삭제/수정도 매일 새벽 자동 갱신에 반영됩니다.
- **이미지·첨부파일은 챗봇이 읽을 수 없습니다.**
  - 노션 페이지에 이미지(사진, 스크린샷, 안내 이미지 등)나 PDF·파일 첨부로만
    올라와 있는 정보는 챗봇이 내용을 인식하지 못합니다.
  - 이미지 안에 텍스트(표, 안내문 등)가 담겨 있어도 마찬가지로 읽지 못하며,
    해당 내용에 대한 질문에는 답변이 불가합니다.
  - **해결 방법**: 이미지로만 제공된 중요 정보는 노션 페이지에 **텍스트로 함께
    작성**해두면 챗봇이 검색해 답변할 수 있습니다.

### a-1) Airtable 연동 현황 및 향후 확장 방안

**현재 상태 (A안 적용 중)**

통합시스템 노션 페이지에는 아래 5개의 Airtable 제출 폼 링크가 포함되어 있습니다.
현재 챗봇은 이 링크들을 노션 페이지 수집 시 텍스트로 포함해 검색에 활용합니다.
(예: "활동계획은 어디서 제출하나요?" → 해당 Airtable 폼 주소를 답변에 포함)

| 폼 링크 | 용도 |
|---------|------|
| `https://airtable.com/appJzoSwTK564BeNU/shrjPyYDUidSpjr70` | 최종 활동계획 제출 |
| `https://airtable.com/appJzoSwTK564BeNU/shrPDEPpXmAIIx3Lt` | Airtable에서 직접 확인 후 기재 |
| `https://airtable.com/appJzoSwTK564BeNU/shrmO4PAfwvlkMBhe` | Airtable에서 직접 확인 후 기재 |
| `https://airtable.com/appJzoSwTK564BeNU/shry21BN1rBDsLNN7` | Airtable에서 직접 확인 후 기재 |
| `https://airtable.com/appJzoSwTK564BeNU/shrbCdt4l6dhr7Xcz` | Airtable에서 직접 확인 후 기재 |

Airtable Base ID: `appJzoSwTK564BeNU` (동아리ON 운영팀 소유)

---

**향후 확장 방안 (B안)**

폼에 제출된 실제 데이터(참여자 신청 내용, 제출 이력 등)를 챗봇이 직접 읽어
답변에 활용하려면 아래 절차가 필요합니다. 코드는 이미 준비되어 있어
설정만 추가하면 동작합니다.

**① Airtable 토큰에 Base 접근 권한 추가**
- [airtable.com](https://airtable.com) → Builder Hub → Personal access tokens →
  "동아리ON 챗봇" 토큰 클릭 → Access → "Add a base" 에서
  `appJzoSwTK564BeNU` Base 선택 후 저장

**② config.json 수정**
- 프로젝트의 `config.json` 파일에서 `airtable_views` 배열에 아래 형식으로 추가
  (수집할 테이블마다 항목 추가, `table_name`은 Airtable에서 실제 테이블 이름 확인):
  ```json
  "airtable_views": [
    {
      "base_id": "appJzoSwTK564BeNU",
      "table_name": "테이블이름",
      "view": "Grid view"
    }
  ]
  ```

**③ Vercel 환경변수에 AIRTABLE_API_TOKEN 추가**
- Vercel 대시보드 → 프로젝트 → Settings → Environment Variables
- `AIRTABLE_API_TOKEN` 추가 (값은 프로젝트 `.env` 파일에 이미 저장된 `patG7EzkcbodDQ7ce...` 값 사용)
- 저장 후 **Redeploy** 필요

**④ 데이터 수집 실행**
- 배포 완료 후 대시보드 "지금 갱신" 버튼 클릭 → Airtable 데이터 즉시 수집 및 검색 반영

### b) 자동 갱신은 하루 1번뿐입니다 (매일 새벽 4시, Vercel 무료 플랜 제약)
- 급하게 반영해야 하면 대시보드의 **"지금 갱신"** 버튼을 누르면 즉시 동기화됩니다.

### b-1) Vercel Pro 플랜 업그레이드 시 추가되는 기능 (현재는 무료 플랜 운영 중)

현재는 Vercel 무료(Hobby) 플랜으로 운영합니다. 아래는 Pro 플랜($20/월)으로 업그레이드할 경우 추가되는 기능입니다. 필수는 아니지만, 운영 규모가 커지면 고려할 수 있습니다.

| 기능 | 무료(Hobby) | Pro |
|------|------------|-----|
| 자동 크론 실행 빈도 | 하루 1번 | 분 단위 가능 (예: 매 10분, 매 시간) |
| Vercel 런타임 로그 보존 기간 | **1시간** | **3일** |
| Vercel 대시보드 접근 계정 수 | 본인 1명 | 팀 멤버 초대 가능 |

**각 기능이 필요해지는 시점:**
- **크론 빈도**: 노션에 자료를 올리고 챗봇에 빠르게 반영되어야 할 때 (현재는 하루 1번 또는 수동 "지금 갱신"으로 운영 중)
- **로그 보존 3일**: 아래 두 단계로 크론 이상을 확인할 수 있습니다.
  - **1단계 — 어드민 패널 배너**: 로그인 후 사이드바의 크론 상태 배너(초록/노랑)로 마지막 실행 시각·성공 여부를 빠르게 확인
  - **2단계 — Vercel 런타임 로그**: 배너에서 이상이 감지되거나(노랑/빨강), 배너는 정상인데 챗봇 답변이 이상할 때, 원인을 코드 레벨에서 추적하려면 Vercel 대시보드의 런타임 로그가 필요합니다. **무료 플랜은 로그가 1시간만 보존**되므로 새벽 4시 크론 로그를 오전에 확인하려 해도 이미 사라진 상태입니다. Pro 플랜(3일 보존)이라면 당일 중 언제든 로그를 열어볼 수 있습니다.
- **팀 멤버**: 운영팀 여러 명이 Vercel 대시보드에 직접 접근해야 할 때

### c) 90일이 지난 질의응답 기록은 자동 삭제됩니다 (개인정보 보호 목적)

질의응답 기록(`qa_log`)은 **90일 경과 후 자동 삭제**됩니다. 삭제는 매일 새벽 자동 크론에서 실행됩니다.

### g) 지식DB(Knowledge Base) 노후화 관리

챗봇이 답변의 근거로 쓰는 문서(지식DB)는 시간이 지나면서 현실과 어긋날 수 있습니다.

- **노후화가 발생하는 이유**: 행사가 종료되거나 일정이 바뀌어도 노션 페이지에 내용이 그대로 남아 있으면 챗봇이 오래된 정보를 계속 안내합니다. 담당자 연락처 변경, 제출 폼 URL 변경, 활동 기준 변경 등도 마찬가지입니다.

- **운영자가 해야 할 일**:
  - 프로그램 일정이 끝났거나 내용이 바뀌었을 때, 해당 노션 페이지를 직접 수정하거나 삭제한 뒤 대시보드 "지금 갱신"을 눌러 챗봇에 반영하세요.
  - 파일·구글시트로 등록한 문서도 주기적으로 점검해 만료된 문서는 삭제하고 최신 문서로 교체하세요.
  - 대시보드 "원인별 집계 리포트"에서 "검색실패" 건수가 갑자기 늘어나면, 지식DB에 최신 정보가 반영되어 있는지 점검하세요.

- **자동 갱신의 한계**: 매일 새벽 자동 동기화는 "노션에 있는 내용을 챗봇에 반영"하는 것이지, 오래된 내용을 스스로 판단해 삭제하지 않습니다. 노션 페이지를 운영자가 최신 상태로 직접 관리하는 것이 전제입니다.

---

### h) Supabase DB 용량 제한

챗봇의 모든 데이터(지식 문서, 임베딩 벡터, 질의응답 기록 등)는 Supabase PostgreSQL에 저장됩니다. Supabase 무료(Free) 플랜의 DB 저장 한도는 **500 MB**입니다.

**한도 초과 시**: Supabase가 해당 프로젝트를 자동 일시정지합니다. 일시정지되면 챗봇과 대시보드가 모두 중단되며, Supabase 대시보드에서 수동으로 복구해야 합니다.

**자동 알림**: Supabase는 사용량이 75%(375 MB), 90%(450 MB)에 도달하면 프로젝트 소유자 이메일로 자동 경고 메일을 발송합니다. 별도 설정 없이 활성화되어 있으나, 프로젝트를 만든 계정의 이메일 수신함을 실제로 확인하는 것이 전제입니다.

**수동 확인 방법**: [supabase.com](https://supabase.com) → 해당 프로젝트 → **Settings → Usage** 에서 현재 사용량을 직접 확인할 수 있습니다.

**용량을 주로 차지하는 항목**:

| 테이블 | 내용 |
|--------|------|
| `documents` | 지식DB 문서 + 임베딩 벡터 (인덱스가 데이터 대비 크게 차지) |
| `qa_log` | 질의응답 기록 (90일 이후 자동 삭제) |
| `cron_run_log` | 자동 동기화 실행 기록 |

지식DB 문서는 수동으로 관리하지 않으면 계속 누적됩니다. 더 이상 사용하지 않는 파일·구글시트 문서는 주기적으로 정리하세요.

---

### i) 챗봇 개인정보 처리

**수집되는 정보**: 사용자가 챗봇 위젯에 입력한 질문 텍스트, 챗봇의 답변, 실패 원인 분류, 응답 속도, 세션 ID가 `qa_log` 테이블에 저장됩니다. 이름·연락처 등 신원 정보는 별도로 요청하거나 수집하지 않지만, 사용자가 질문 내용에 직접 포함시킬 경우 그 내용도 로그에 남습니다.

**세션 식별**: 동일 사용자 판별에 브라우저 세션 ID를 사용합니다. 로그인 없이 익명으로 동작하며, 탭/창을 닫으면 세션이 초기화됩니다.

**보관 기간**: 질의응답 기록은 **90일 후 자동 삭제**됩니다.

**열람 가능 범위**: 대시보드 계정을 가진 운영자는 "질의-답변 연계조회" 메뉴에서 최근 90일 이내의 모든 질의응답 내용을 열람할 수 있습니다.

**운영자가 확인해야 할 사항**:
- 챗봇 위젯이 임베드된 노션 페이지에서, 질문 내용이 로그에 기록된다는 사실을 사용자에게 안내하는 것이 적절한지 판단하세요.
- 대시보드 접근 계정(이메일·비밀번호)이 외부에 유출되지 않도록 관리하세요. 대시보드에서 모든 질의응답 내용을 열람할 수 있습니다.
- 의심스러운 로그인 시도가 확인되면 즉시 비밀번호를 변경하세요.

---

### d) 코드 수정 없이 운영자가 바로 바꿀 수 있는 것 (대시보드에서 즉시 반영)
- 담당자 연락처, 신뢰도 판단 기준, 욕설/혐오 표현 사전, 질문 상황 분류 키워드,
  최대 질문 글자수, 분당 요청 제한(같은 사용자가 1분에 몇 번까지 질문 가능한지) 등
  — 대시보드 "챗봇 운영지침" 메뉴에서 저장하면 재배포 없이 바로 다음 질문부터 반영됩니다.

### e) 코드/설정 수정이 필요한 작업 (재배포 필요)

운영 중 아래 작업은 GitHub 저장소의 파일을 직접 수정한 뒤 Vercel에 재배포해야 반영됩니다.

| 작업 | 수정 파일 | 변경 내용 |
|------|----------|----------|
| 새 노션 최상위 페이지 추가 연결 | `config.json` | `notion_pages`에 URL 추가 (단, 메인페이지 하위에 넣을 수 있으면 추가 불필요) |
| Airtable 직접 연동 (B안) | `config.json` | `airtable_views`에 항목 추가 + Vercel 환경변수 `AIRTABLE_API_TOKEN` 등록 |
| 구글 스프레드시트 연결 | `config.json` | `google_sheets`에 공개 URL 추가 |
| 욕설/혐오 표현 사전 추가 | `forbidden_words.json` | 단어 추가 |
| 카테고리 분류 기준 변경 | `config.json` | `categories` 항목 수정 |

**재배포 방법**: GitHub에서 해당 파일을 직접 수정 → 커밋 → Vercel이 자동으로 재배포합니다.
(Vercel 프로젝트가 GitHub 저장소와 연결되어 있으면 푸시만으로 자동 배포됩니다.)

### f) Voyage AI 결제수단 등록 (필수 권장)
- 챗봇이 노션 문서를 검색용으로 변환(임베딩)할 때 Voyage AI를 사용합니다.
  결제수단이 등록되지 않은 계정은 **분당 3건, 분당 1만 토큰**으로 제한되는데,
  노션 동기화 시 한 번에 갱신되는 문서가 많으면 이 제한에 걸려 일부 문서가
  **검색에 반영되지 않고 조용히 실패**할 수 있습니다(에러 화면 없이 그냥 안 보임).
- [Voyage AI 대시보드](https://dashboard.voyageai.com/) → 결제(Billing) 페이지에서
  결제수단을 등록하면 제한이 크게 늘어납니다. 무료 토큰(voyage-3 계열 기준 2억
  토큰)은 결제수단 등록 후에도 그대로 적용되니 비용 걱정 없이 등록해도 됩니다.

---

## 5. 초기 배포 전 확인 체크리스트

- [ ] GitHub 저장소 `seoulclub-on/26seoulclub`에 최신 코드 확인 완료
- [ ] `JWT_SECRET_KEY` 운영용으로 새로 발급 완료
- [ ] `python create_admin_account.py`로 최초 관리자 계정 생성 완료
- [ ] Voyage AI 결제수단 등록 완료 (4-f 참고)
